import { AuthDialogButton } from "@/components/auth-dialog-btn";

const HomePage = async () => (
  <main>
    <section className="min-h-screen flex justify-center items-center">
      <AuthDialogButton />
    </section>
  </main>
);

export default HomePage;
